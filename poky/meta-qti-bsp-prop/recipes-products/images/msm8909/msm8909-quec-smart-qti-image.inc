# Packages that quec-smart installed to the root file system

require msm8909-qti-image.inc

IMAGE_INSTALL += "fastmmi"
IMAGE_INSTALL += "abctl"
IMAGE_INSTALL += "start-scripts-le-updater"
IMAGE_INSTALL += "mcm-core"
IMAGE_INSTALL += "mcm"
