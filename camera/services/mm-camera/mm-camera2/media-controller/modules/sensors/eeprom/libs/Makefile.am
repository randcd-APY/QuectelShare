# ---------------------------------------------------------------------------
#                       Make the shared library eeprom
# --------------------------------------------------------------------------- 
ACLOCAL_AMFLAGS = -I m4

dirs =
dirs += eeprom_util
dirs += le2464c
dirs += onsemi_cat24c32
dirs += imx378_gt24c64

SUBDIRS = $(dirs)
